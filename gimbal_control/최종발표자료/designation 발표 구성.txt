designation 발표 구성

Wc=27을 초과하면 saturation이 걸리기 시작함 -> 데이터 보여주자.
Wc=15->20->25->27일때 step response plot 순서대로 보여주기
그리고 이걸 모두 simulink(비선형요소 포함) plot이랑 함께 plot하기

Wc=30일 때 tr=0.100으로 만족하지만 saturation이 걸림 -> 데이터 신뢰하기 어려움
그리고 Wc를 더 올렸을 때는 saturation 때문에 tr가 오히려 커지는 것을 볼 수 있었음


그러면 모델링을 잘못한거 아니니ㅑ?! (= 1차로 모델링 했기 때문에 마진이 너무 적은것 아니냐)
그래서 우리가 2차로 모델링을 다시 해봤습니다
하지만! 2차로 하고 1차로 근사를 햇음에도 bandwidth가 (수치로 보여주기) 크게 늘지 않음 -> 여전히 스펙 못함

+) 갑자기 2차로 하고 나서 oscillation이 미친듯이 생김
-> 진동하는 주파수를 없애면 되지 않겟느냐!
-> 하지만 측정할때마다 진동하는 주파수성분이 매번 달라짐 & 그 주파수를 예측할 수 없음 -> 

=> 그러던 와중!! 우리가 김발 헤드가 잘 체결이 되지 않아서 쉽게 흔들거리는 문제가 있음을 발견

호오옥시 기어가 헐렁하기 때문에 큰 각속도 입력을 주었을 때 관성에 의해 재빠르게 목표각도에서 멈추지 못하고 더 가서 oscillation이 생기게 된 것인건 아닐까?????
하고 기어를 다시 조임.
놀랍게도 oscillation이 사라짐
백래시 기어 체결이 헐렁햇음

그런데 모터를 다시 체결했으니 전달함수가 바뀌었을 가능성 추측 + 어차피 2차도 별로 bandwidth가 커지지 않음
=> 전달함수를 다시 추정


~~~ 태초마을 ~~~
Wc=27을 초과하면 saturation이 걸리기 시작함 -> 데이터 보여주자.
Wc=15->20->25->27일때 step response plot 순서대로 보여주기
그리고 이걸 모두 simulink(비선형요소 포함) plot이랑 함께 plot하기

Wc=30일 때 tr=0.100으로 만족하지만 saturation이 걸림 -> 데이터 신뢰하기 어려움
그리고 Wc를 더 올렸을 때는 saturation 때문에 tr가 오히려 커지는 것을 볼 수 있었음